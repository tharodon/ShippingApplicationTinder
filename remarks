Ломается на спецсимволах
Проверка на волны (СуДаРь)
Main pom.xml
Ключевые слова сделать капсом

Id - поменять на chat_id_telegram
Сделать комменты к таблицам  (Alter table add комментарий к каждой колонке)
Сделать переменные более читаемыми
Объединить в одну таблицу лайки и дизлайки

---------------CLIENT------------------------

Исправить реализацию Logging. Сделать Exception Handler.
FieldDefault убрать. Написать модификаторы ручками.

Property_source
В конструкторе BotMassage просто заинжектить Telegram config.

ReplyKeyboardMaker инжектить, убрать "new".
Поставить модификаторы доступа.
map - поменять имя.
Убрать final строка ReplyKeyboardMaker.

Убрать дубликаты кода в кнопках.
Создать константы для названий кнопок.

Url брать из property.
url убрать из логов

Поставить @RequieredConstructor над RequestRunner

вместо getForEntity getForObject

В MessageHandler правильно и грамотно упаковать переменные (@Autowired, private final)
equals("") поменять на isEmpty().

ThreadLocal

Переименовать пакеты и названия классов

Попробовать сделать логику через интерфейсы

Переопределить equals и hashcode

Исправить составление форм

Пути к картинкам заменить на getFile или getResource

writeToFile вынести в отдельный класс.














